description = "Verify and recommend an MLX model for a requested role."
prompt = "Activate and follow the bundled mlx-adopt skill.\n\nUse Gemini's native skill activation and orchestration when it is available. Otherwise use the skill's sequential resumable adoption core with a visible state path. Do not download model weights or change configuration. {{args}}"
